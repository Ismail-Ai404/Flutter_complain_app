import 'package:flutter/material.dart';

class LogOfComplainsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Empty'),
    );
  }
}
